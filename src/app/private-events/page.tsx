import React from "react";
import PrivateEvents from "@/sections/private-events/PrivateEvents";
const page = () => {
  return (
    <div>
      <PrivateEvents />
    </div>
  );
};

export default page;
